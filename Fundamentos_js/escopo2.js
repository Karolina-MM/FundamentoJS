if (1 > 0) {
    let estudante = "Karol"
    console.log(estudante);
  }
  
  estudante = "Naiana"
  console.log(estudante);

  //Neste caso, temos 2 variáveis diferentes.

  //Se cria a váriavel sem passar nenhuma palavra chave (let, const ou var)
  //ela não existe automaticamente o JS cria a varável com um "var" escondido.

  //JavaScript trabalha com três escopos: Bloco; Função; Global
  //Um escopo é um termo em programação que normalmente significa um limite, um espaço dentro do qual trabalhamos.